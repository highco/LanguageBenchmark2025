Take the main.js benchmark written in JavaScript and translate it to the Zig programming language.
Do not change the classes, data structures, and the benchmark function to keep the benchmark fair!
Heavyly optimize the joinRoom and addUserInputs function to make is as fast and performant as possible!
Build an executable called "bin/zig" optimized for maximum performance and maximum execution speed.
https://ziglang.org/learn/